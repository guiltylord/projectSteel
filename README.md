# projectSteel

Универсальный парсер с автоматическим прохождением капч (OCR) на базе Playwright и ddddocr.

## 📋 Описание

Проект предназначен для автоматизации взаимодействия с веб-сайтами: заполнение форм, клики, обход капч и сохранение результатов.

## 🏗️ Структура проекта

```
projectSteel/
├── main.py           # Основной движок парсера
├── converter.py      # Конвертер кода из Playwright Codegen в сценарии
├── inspector.py      # Интерактивный инструмент для сбора селекторов
├── scenarios.json    # Конфигурация сценариев парсинга
├── requirements.txt  # Зависимости проекта
└── records/          # Папка для сохранённых записей
```

## 📦 Установка

1. Установите зависимости:

```bash
pip install -r requirements.txt
```

1. Установите браузеры Playwright:

```bash
playwright install chromium
```

1. Требуется Python 3.10+

## 🚀 Использование

### Запуск парсера (новая версия — feature/scenario)

```bash
python main.py -s <имя_сценария> -d <аргумент1> <аргумент2> ...
```

**Параметры:**

- `-s` — имя сценария из `scenarios.json`
- `-d` — данные для передачи в сценарий (через пробел)

**Пример:**

```bash
python main.py -s scenario_name -d "Иванов" "Иван" "Москва"
```

### Запуск парсера (старая версия — main)

Настройте `.env` файл:

```env
C0=https://example.com
C1=#field1
C2=#field2
C3=#field3
C4=#list
C5=#date
C6=#trigger
C7=#captcha-input
C8=#captcha-img
C9=5
C10=#submit-btn

D1=Значение1
D2=Значение2
D3=Значение3
D4=Значение4
D5=Значение5
```

Запуск:

```bash
python main.py
```

### Создание сценариев

#### Способ 1: Через `converter.py` (рекомендуется)

1. Запишите действия в Playwright Codegen
2. Скопируйте сгенерированный код
3. Запустите конвертер:

```bash
python converter.py
```

1. Вставьте код и следуйте инструкциям

#### Способ 2: Через `inspector.py` (интерактивный)

1. Запустите инспектор:

```bash
python inspector.py
```

1. Введите URL сайта
2. Наводите курсор на элементы и вводите команды:
  - `f` — поле ввода (Fill)
  - `s` — выбор из списка/дата (JS Set)
  - `c` — клик (Click)
  - `ci` — картинка капчи
  - `cn` — поле ввода капчи
  - `cs` — кнопка отправки капчи
  - `r` — обновить подсветку
  - `q` — выход и сохранение

### Формат `scenarios.json`

```json
{
  "scenario_name": {
    "url": "https://example.com",
    "actions": [
      {
        "type": "fill",
        "selector": "placeholder:Поиск",
        "arg_index": 0
      },
      {
        "type": "click",
        "selector": "role:button[name='Найти']"
      }
    ],
    "captcha": {
      "image": "#captcha-img",
      "input": "#captcha-input",
      "submit": "role:button[name='Отправить']",
      "refresh": "#captcha-refresh",
      "max_tries": 5
    }
  }
}
```

**Типы действий:**

- `fill` — заполнение поля
- `click` — клик по элементу
- `press` — нажатие клавиши

**Форматы селекторов:**

- `role:button[name='Текст']` — по роли ARIA
- `placeholder:Текст` — по placeholder
- `text:Текст` — по тексту
- `#id`, `.class` — обычные CSS-селекторы

## 🔧 Особенности

- **Автоматическое прохождение капч** с помощью OCR (ddddocr)
- **Поддержка React/Vue** — эмуляция событий ввода для фреймворков
- **Гибкая система селекторов** — кастомные и стандартные CSS
- **Многократные попытки** — автоматическое обновление капчи при неудаче

## 📁 Вывод

При успешном выполнении результат сохраняется в:

- `out.html` — для новой версии (feature/scenario)
- `capture_result.html` — для старой версии (main)

## 📝 Зависимости

- `playwright` — автоматизация браузера
- `ddddocr` — распознавание капч
- `python-dotenv` — загрузка .env (для старой версии)
- См. `requirements.txt` для полного списка

## 🌿 Ветки


| Ветка                       | Описание                                                       |
| --------------------------- | -------------------------------------------------------------- |
| `main`                      | Стабильная версия с конфигурацией через `.env`                 |
| `feature/scenario`          | Новая версия с динамическими сценариями через `scenarios.json` |
| `feature/inspector`         | Инструмент для интерактивного сбора селекторов                 |
| `feature/improve-inputting` | Улучшенная система ввода для React/Vue                         |


